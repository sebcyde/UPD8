import React from 'react';
import styled from 'styled-components';
import { SettingsContainer } from '../Styles/AllComponents';

function Settings() {
	return (
		<SettingsContainer>
			<h2>Settings</h2>
			<div>
				<span>Settings</span>
				<span>Settings</span>
				<span>Settings</span>
				<span>Settings</span>
				<span>Settings</span>
				<span>Settings</span>
				<span>Settings</span>
				<span>Settings</span>
			</div>
		</SettingsContainer>
	);
}

export default Settings;
